/**
 * Deduplication Module Index
 * ==========================
 *
 * Re-exports all deduplication functions for easy importing.
 *
 * Usage:
 *   import { matchDedup, teamDedup, eventDedup } from './deduplication/index.js';
 */

export * as matchDedup from './matchDedup.js';
export * as teamDedup from './teamDedup.js';
export * as eventDedup from './eventDedup.js';
