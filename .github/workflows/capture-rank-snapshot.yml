name: Daily Rank Snapshot

on:
  schedule:
    # Run daily at noon UTC (6 AM CST) — AFTER daily-data-sync completes
    # daily-data-sync starts at 6 AM UTC and takes ~1-2 hours
    # Snapshot must capture POST-update ranks, not stale pre-update data
    - cron: "0 12 * * *"
  workflow_dispatch: # Allow manual trigger

env:
  SUPABASE_URL: ${{ secrets.SUPABASE_URL }}
  SUPABASE_SERVICE_ROLE_KEY: ${{ secrets.SUPABASE_SERVICE_ROLE_KEY }}

jobs:
  capture-snapshot:
    runs-on: ubuntu-latest
    timeout-minutes: 10

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "20"
          cache: "npm"

      - name: Install dependencies
        run: npm ci

      - name: Capture Rank Snapshot
        env:
          SUPABASE_URL: ${{ secrets.SUPABASE_URL }}
          SUPABASE_SERVICE_ROLE_KEY: ${{ secrets.SUPABASE_SERVICE_ROLE_KEY }}
        run: node scripts/daily/captureRankSnapshot.js

      - name: Summary
        run: echo "✅ Daily rank snapshot captured at $(date -u)"
